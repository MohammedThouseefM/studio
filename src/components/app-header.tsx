'use client';

// This component is obsolete and has been replaced by the new dashboard layout.
// It is kept to prevent breaking any potential remaining imports, but it renders nothing.
export function AppHeader() {
  return null;
}
